# Bash Scripting Practice Programs

This repository contains a collection of basic to intermediate Bash scripts designed to strengthen your understanding of syntax, loops, conditionals, and file operations in shell scripting.

## 🧰 Topics Covered

- Basic Syntax
- Conditional Statements (`if`, `case`)
- Loops (`for`, `while`, `until`)
- File and Directory Operations
- System Information and User Management

---

## 📜 Script List

| #  | Script Description                                 | Concepts Used                             |
| -- | -------------------------------------------------- | ----------------------------------------- |
| 1  | **Hello World script**                       | Echo, syntax basics                       |
| 2  | **Print current date and time**              | `date` command                          |
| 3  | **Display system hostname**                  | `hostname` command                      |
| 4  | **Add two numbers**                          | Arithmetic operations                     |
| 5  | **Find the largest of three numbers**        | `if-elif-else`, comparison operators    |
| 6  | **Check if a number is even or odd**         | Modulo operator, conditionals             |
| 7  | **Print all files in the current directory** | `ls`, `for` loop                      |
| 8  | **Count the number of files in a directory** | `find`, `wc`, wildcards               |
| 9  | **Read a file line by line**                 | `while read` loop                       |
| 10 | **Check if a file exists**                   | `-f` conditional                        |
| 11 | **Create a backup of a file**                | `cp`, string manipulation               |
| 12 | **Compare two strings**                      | `==` operator, `if` statement         |
| 13 | **Use case statement (simple menu)**         | `case`, `select`, `read`            |
| 14 | **Print a multiplication table**             | `for` loop, arithmetic                  |
| 15 | **Calculate factorial of a number**          | `while` loop, arithmetic                |
| 16 | **Use read to get user input**               | `read`, variables                       |
| 17 | **Append text to a file**                    | `echo`, `>>` redirect                 |
| 18 | **Check if a directory exists**              | `-d` conditional                        |
| 19 | **List logged-in users**                     | `who` command                           |
| 20 | **Check disk usage**                         | `df -h`                                 |
| 21 | **Check memory usage**                       | `free -h`, `top`                      |
| 22 | **Check if a user exists**                   | `/etc/passwd`, `grep`, `cut`        |
| 23 | **Check if a package is installed (apt)**    | `dpkg -l`, `grep`                     |
| 24 | **Rename multiple files in a folder**        | `for` loop, `mv`, string manipulation |
| 25 | **Find files larger than 1 GB**              | `find`, `-size +1G`                   |
| 26 | **Display current working directory**        | `pwd` command                           |
| 27 | **Create and delete a directory**            | `mkdir`, `rmdir`                      |
| 28 | **List hidden files**                        | `ls -a`, filters                        |
| 29 | **Countdown timer using loop**               | `sleep`, `while` or `for` loop      |
| 30 | **Simple password prompt using read -s**     | `read -s`, conditional                  |

---

## 🏃 Getting Started

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/bash-scripting-exercises.git
   cd bash-scripting-exercises
   ```
